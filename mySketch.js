function setup() {
  createCanvas(windowWidth, windowHeight);
  background(34, 139, 34);
}

function draw() {
  background(34, 139, 34);
  fill(255);
  textSize(32);
  textAlign(CENTER, CENTER);
  text("it works", width/2, height/2);
}
